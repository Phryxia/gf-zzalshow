export type MetaInfo = {
  fileUrl: string;
  width: number;
  height: number;

  sampleUrl: string;
  sampleWidth: number;
  sampleHeight: number;

  previewUrl: string;
  previewWidth: number;
  previewHeight: number;

  tags: string[],
  createdAt: Date
};